package team02_KostaEducation2.service;

public class ManageService {
	
	
	public String bestAttendance(Student[] student) {
		for(Student s : student) {
			int max = s.getAttendance();
			if(max<s.getAttendance())
		}
	}
	
	
}
public int getLowPrice(ProductVO[] pros) {
	for(ProductVO v : pros) {
	int min = v.getPrice();
	if(min<v.getPrice())
		min = v.getPrice();
}return getLowPrice;
}